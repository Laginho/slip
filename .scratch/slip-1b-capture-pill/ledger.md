# Ledger — slip-1b-capture-pill

Written only by master (see `docs/agents/orchestration.md`). One line per cycle, appended
after master validation; cycle branches never touch this file.

| cycle | issue | verdict | culprit | reason |
| ----- | ----- | ------- | ------- | ------ |
| 01 | 01-line-breaks-store-card-editor | clean | - | TEST `fe82c13` (big pickle): 20 rows — store normalisation 1–8 plus 8b, the textarea editor 9–18 plus the absent-`matchMedia` error case, Ctrl+H row 7 rewritten to type into the editor and assert the draft intact; `typeInto` widened to textareas. MAKE `bf39a00` (muse spark 1.3 free): one normalisation shared by `create`/`editText` (CRLF/CR → LF, whitespace-only lines emptied, trim, three or more breaks → two), `useMediaQuery(query)` replacing the two hand-written subscriptions in `App.tsx` and serving the Card, `<textarea>` editor with `rows` from the draft's line count, Enter commits only under `(pointer: fine)` and without Shift, blur commits, Escape cancels, Ctrl+H guard now the one textarea inside an `li`. READ `e48e04d` (mimo v2.5 free): 245/245, `tsc -b`, no hex outside `palette.ts`, no `window.matchMedia` left in `App.tsx`, no refactor. Master nit `16d8046` (`Role: MASTER`): the hook's `addEventListener` presence check existed only for two Card tests stubbing a bare `{ matches, media }`; those stubs now use `stubMediaWithChangeListener` and the hook subscribes unconditionally. Master re-ran the gates in the worktree (245/245, `tsc -b`). Browser at 1280px: editing a Task to `a\nb\n\n\n\nc` stores `a\nb\n\nc`, the Card renders the breaks, Enter is prevented and commits, Shift+Enter is not prevented; under mobile emulation blur commits a four-line draft; coarse Enter rests on unit row 13 (the pane delivers no keydown under touch emulation). PR #20, merged 2026-09-04 (`824743a`). This cycle's handoffs were not kept in this tree; PR #20's description is the record. |
| 02 | 02-pill-textarea-enter-send | correction | TEST (big pickle) | TEST `97779fd` (big pickle): the 27-row pill matrix plus the legacy rewrites, `src/App.test.tsx` only, 272 tests / 31 red on the base. MAKE (muse spark 1.3 free) implemented handoff 01's Design verbatim in one attempt (`tsc -b`, `npm run build` green, 268/272) and stopped at the role boundary on four test-only defects, no commit: the "getItem throws" row still selected `input[placeholder="uma tarefa..."]` (line 259 was missing from master's legacy-selector list, so TEST and master share this one), and rows 12–14 asserted the `enterKeyHint` IDL property, which jsdom 29 does not reflect, where handoff 01 offered `getAttribute("enterkeyhint")`. READ did not run. Master verified base, trailer, red count and the four failures in the worktree, saved MAKE's diff as `handoffs/02-make-attempt1.patch`, fast-forwarded `feat/02-pill-textarea-enter-send` to `97779fd`, disposed of `traycer/radiant-seal`. Correction handoff 05, return 06. |
| 03 | 02-pill-textarea-enter-send | clean | - | Correction re-run of cycle 02 (handoff 05, return 06). TEST `0ef7ca9` (big pickle): the two prescribed fixes only — the legacy storage-read selector at App.test.tsx:259 now picks the textarea, rows 12–14 read `getAttribute("enterkeyhint")`; red count kept at 31 failed / 241 passed with the planned composition change. MAKE `b122dfe` (muse spark 1.3 free): reapplied its own cycle-02 patch cleanly — one-render-path pill (`calc(100% - 32px)`, 720px cap, `--capture-bg`, radius 999/26 by line count), `<textarea>` with `rows` from the line count capped at 5, `useMediaQuery("(pointer: fine)")` driving Enter/`enterKeyHint`/launch focus, submit-only send button (36px circle, paper plane, dimmed when blank), day placeholder "dd", raw `text` to `onCapture`, `wide` prop dropped in App.tsx, DESIGN.md/PRODUCT.md scoped edits. READ (mimo v2.5 free): 272/272, `tsc -b`, `npm run build`, file set and greps exact, no refactor, no commit. Master nit `26eab73` (`Role: MASTER`): reflow of the three prose lines the cycle touched. Master re-ran the gates in the worktree and browser-checked 1280px light (pill 720px centred, growth 999→26px, five rows then scroll, Enter sends, Shift+Enter breaks, Alt+1/2 flip the chip, Ctrl+H from the textarea, refused write keeps text/day/Kind, button sends a three-line Task rendered on three lines) and 390px dark (16px gutters, `enterkeyhint="enter"`, Enter not prevented, no launch focus, dark ground, light circle) via JS-driven checks. PR #21, merged 2026-09-04 (`a0c67ab`). |
