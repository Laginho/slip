# Ledger — slip-1a-chrome-archive

Written only by master (see `docs/agents/orchestration.md`). Cycle branches carried their own
copies until 2026-09-02; those rows are consolidated here with unique numbers.

| cycle | issue | verdict | culprit | reason |
| ----- | ----- | ------- | ------- | ------ |
| 01 | 01-rename-slip-portrait | clean | - | TEST red, MAKE green, READ gate all passed. PR #10. |
| 02 | 02-card-44px-controls | correction | MAKE (opencode:muse-spark-1.2-contributor-free); READ (opencode:mimo-v2.5-free) | MAKE compensated the 44px targets only vertically (`margin: "-13px 0"`) and changed the row gaps (wall 10→4, bubble 4→2, dead `bubble ? 4 : 4`); each control grew to 44px in flow. Master measured on the wall: text column 209→80px, Card heights 86→113 and 113→275. Phone bubble unaffected. READ signed off "no plan deviations". TEST rows 1–8 correct. Correction: handoff 09. |
| 03 | 04-archive-at-top | correction | TEST (opencode:mimo-v2.5-free); MAKE (opencode:muse-spark-1.2-contributor-free) | TEST left the two legacy `"nada por aqui"` assertions (`App.test.tsx:78`, `:247`) untouched, so MAKE ended 158/2 and READ never ran; rows 1 and 5 also demand the Open list be a single `<main>` child. MAKE satisfied that with an unplanned `<div>` wrapper (layout change). Archive/TaskList/state lifting correct. PLAN: commit bodies carry a literal `\n\n`, trailers unparseable. Correction: handoff 11, return 12. |
| 04 | 03-dark-chrome-system-scheme | correction | TEST (opencode:mimo-v2.5-free) | After two TEST attempts the legacy jsdom `getComputedStyle(form).backgroundColor` white assertion still blocked a pure `var(--capture-bg)`; MAKE attempt 2 shimmed `getComputedStyle` in `testing.tsx` (out of role, rejected, not committed). Only the two TEST commits exist. Matrix rows 3, 6, 8, 9, 11 of the ticket were never translated. Correction: handoff 13, return 14. |
| 05 | 02-card-44px-controls | clean | - | Correction of cycle 02 (handoff 09, return 15). TEST added rows 9–11 red on the base for the stated reasons; MAKE changed only `actionStyle.margin` to `-13px -12px` and restored `gap: bubble ? 4 : 10` / bubble `gap: 4` byte-for-byte with `main`; READ passed 161/161 and `tsc -b`, no refactor. Master measured both profiles against `main` at 1200px and 390px: Card heights identical (194/86/248 wall, 144/90/177 bubble), text column −5/−4px (tolerance 12), controls 44×44 with 20×18 in flow. Branch rebased onto `main` after PR #10; stale ledger copies dropped at rebase. PR #11. |
| 06 | 04-archive-at-top | correction | master (matrix row 10) | Correction of cycle 03 (handoff 11, return 12). TEST `628b075` fixed the two legacy assertions and rows 1/5 as planned; MAKE `d7f492f` removed the wrapper. Suite 159/1: row 10 (cycle 03's `a495f01`, untouched here) asserts `<main>` keeps 2 children after completing the only Open Task, but an emptied Open list renders nothing (ticket row 6), so 2→1 is correct. Master's matrix row said "children count unaffected"; it passed in cycle 03 only because of the wrapper. MAKE stopped honestly after two attempts, READ not spawned. Trailers now parseable. Master verified 159/1 and `tsc -b` in the worktree. `feat/04` fast-forwarded to `d7f492f`. Correction: handoff 16, return 17. |
| 07 | 04-archive-at-top | clean | - | Correction of cycle 06 (handoff 16, return 17). TEST `0dfeb10` rewrote row 10 as planned (toast outside `main`, no Open list, one child, Archive first) and was green on the base; MAKE skipped as planned; READ gate 160/160 and `tsc -b`, refactor `d4eaee9` collapsed the scrollTo guard. Master: `feat/04` rebased onto post-#10 `main` (stale ledger commits dropped), 168/168, `tsc -b` green. Browser at 390px and 1200px: `<main>` is `P` (Archive) then `UL` (grid on the wall), Card heights 69/69 and 59/59 equal to base, scrollTop 120→0 on open, empty list renders nothing, 0 Open/1 Done shows only the link, completing the last Open Task leaves one child with the toast fixed outside `main`. Observation: the top-right toast covers the Archive link while shown. PR #12. |
| 08 | 03-dark-chrome-system-scheme | correction | TEST (opencode:mimo-v2.5-free); master (handoff 13 row 13) | Correction of cycle 04 (handoff 13, return 14). TEST `f622a22` replaced the legacy computed-colour assertion and added rows 9–13 as planned, but left an unused `CHROME` local in row 9 (`App.test.tsx:942`, `tsc -b` TS6133) and asserted the nine-swatch freeze sentence in DESIGN.md, where it never was: on `main` it is `PRODUCT.md:63`. Master's handoff named the wrong file; TEST did not "locate the current wording first". MAKE `de02909` implemented the plan across the nine files: `Card.tsx` untouched, seven root properties, both `matchMedia` listeners cleaned up, hex scan clean (master pre-checked); MAKE attempt 1 excluded tests in `tsconfig.json`, rightly rejected by PLAN. Suite 195/1, READ not launched. `feat/03` fast-forwarded to `907f06a`. Correction: handoff 18, return 19. |
| 09 | 03-dark-chrome-system-scheme | clean | - | Correction of cycle 08 (handoff 18, return 19). TEST `71e2f2c` removed the unused `CHROME` local and pointed row 13 at `PRODUCT.md`, two test files only, straight to green; MAKE skipped as planned; READ ran the full handoff-13 checklist, no findings, no refactor. Master re-ran the gates in the worktree: 196/196, `tsc -b`, `npm run build` with both theme-color metas (`#f5f4f2`, `#1c1b19`). Browser, feat/03 vs `main` seeded with the same nine Kind×Urgency Tasks: all nine Card backgrounds and inks and the Overdue red byte-identical in light and dark; dark root `#1c1b19`/`#f2f0ec`, capture `#262523`, hairline `#3a3835`, quiet `#a8a49e`; toast `#f7f6f4`/`#2b2a28` in dark, identical to `main` in light. Live flip not reproducible: the Browser pane's scheme emulation changes `matches` but dispatches no `change` event even to a raw listener; the listener path is covered by `dark chrome` row 8. `feat/03` fast-forwarded to `71e2f2c`. |
| 10 | 05-pull-to-reveal-archive | clean | - | TEST `5a74404` added `stubScrollTop()` and rows 1–10, amended ticket-04 row 3 as planned (red exactly where handoff 20 predicted); MAKE `481e86e` region/content split, `ARCHIVE_ROW_HEIGHT`/`ARCHIVE_HIDDEN_OFFSET`, one scroll helper, effect on `[archiveOpen]`, `App.tsx` and `Archive.tsx` only; READ `f0c0a44` no refactor, every structural check passed. Master re-ran the gates in the worktree: 206/206, `tsc -b`, `npm run build`. Browser at 390px and 1200px vs `main` seeded 2 Open/1 Done: at rest `scrollTop` 60 with the link bottom at 0 (just out of view), first Card 12px below the region top, region scrollable by exactly 60px, Card heights 69/69 and 59/59 equal to `main`; pull to 0 reveals the link and nothing opens; open → 0, close → 60; complete + undo leaves 60; 0 Done identical to `main` (no link, no `minHeight`, not scrollable); 0 Open/1 Done hidden by the same mechanics. Observation: the always-scrollable region shows a permanent 15px classic scrollbar on Windows desktop even with two Cards, and at exactly 1200px the grid drops from 4 to 3 columns (1153 < 1168 available); overlay-scrollbar platforms and touch unaffected. Pull-to-refresh not reproducible in the Browser pane; `overscroll-behavior: contain` computed on the region. `feat/05` fast-forwarded to `f0c0a44`. PR #14. |
