# Copy to .env.local and fill in. .env.local is untracked and must never appear in a diff.
# Used by src/sync.ts (issue 09) only. The app is fully usable with these unset.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
